services:
  kwork-parser:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: kwork-parser
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
